__pycache__/
*.pyc
.pytest_cache/
out/
docs/superpowers/

# Real system maps describe internal company architecture — never commit
# them to this (public) repo. Keep them local or in a private repo and
# point ChangeAtlas at them with --graph-data / --base-dir.
graph-data.json
config/component-globs.json
# ...but the bundled fictional samples are public by design.
!sample/**/graph-data.json

# brainstorming companion mockups (local only)
.superpowers/brainstorm/
